This folder stores images for the Styld app.

Currently the app uses remote Unsplash images referenced from `src/assets/data.js` and `/src/components/hero.jsx`.

Recommended hero image filename: `hero.jpg` (suggested size: 1600x900). Place it here to use a local asset instead of the Unsplash URL.
